export interface BankProvider { submitCreditApplication(input: { orderId: string }): Promise<{ externalApplicationId: string; status: string }>; getApplicationStatus(externalApplicationId: string): Promise<{ status: string }>; calculateCredit(input: { amount: number; termMonths: number }): Promise<{ monthlyPayment: number }>; cancelApplication(externalApplicationId: string): Promise<void>; }
