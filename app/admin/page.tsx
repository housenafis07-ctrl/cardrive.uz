import { AdminCard } from "@/components/admin-ui";
import { AdminCatalogService } from "@/services/admin-catalog-service";
import { AdminSalesAnalyticsRepository } from "@/repositories/admin-sales-analytics-repository";
export const metadata={title:"Admin | Cardrive.uz"};
const money=(n:number)=>new Intl.NumberFormat("uz-UZ").format(Math.round(n))+" UZS";
export default async function AdminPage(){const [counts,analytics]=await Promise.all([new AdminCatalogService().counts(),new AdminSalesAnalyticsRepository().get()]);return <><p className="text-sm font-bold text-amber-600">DASHBOARD</p><h1 className="mt-1 text-3xl font-black">Marketplace holati</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><AdminCard label="Brendlar" value={counts.brands}/><AdminCard label="Modellar" value={counts.models}/><AdminCard label="Avtomobillar" value={counts.cars}/><AdminCard label="Tavsiya etilganlar" value={counts.featured}/><AdminCard label="Mavjud" value={counts.available}/><AdminCard label="Tez orada" value={counts.comingSoon}/></div><section className="mt-10"><p className="text-sm font-bold text-amber-600">SOTUV ANALITIKASI</p><h2 className="mt-1 text-2xl font-black">Savdo va moliyalashtirish</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><AdminMetric label="Sotilgan avtomobillar" value={String(analytics.salesCount)}/><AdminMetric label="Jami sotuv" value={money(analytics.salesAmount)}/><AdminMetric label="Moliyalashtirish" value={money(analytics.financingAmount)}/><AdminMetric label="Boshlang‘ich to‘lov" value={money(analytics.downPaymentAmount)}/><AdminMetric label="O‘rtacha sotuv" value={money(analytics.averageSale)}/><AdminMetric label="Kredit savdolari" value={String(analytics.creditSales)}/><AdminMetric label="Rassrochka savdolari" value={String(analytics.installmentSales)}/><AdminMetric label="Naqd savdolar" value={String(analytics.cashSales)}/></div><div className="mt-6 grid gap-5 xl:grid-cols-2"><AnalyticsTable title="Banklar bo‘yicha" rows={analytics.byBank} amountLabel="Moliyalashtirish"/><AnalyticsTable title="Dilerlar bo‘yicha" rows={analytics.byDealer} amountLabel="Sotuv summasi"/><AnalyticsTable title="Kredit dasturlari bo‘yicha" rows={analytics.byProgram} amountLabel="Moliyalashtirish"/><AnalyticsTable title="Savdo turi bo‘yicha" rows={analytics.byPurchaseType} amountLabel="Sotuv summasi"/></div><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black">Sotuv voronkasi</h3><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{analytics.byStatus.map(row=><div key={row.name} className="rounded-xl bg-slate-50 p-4"><p className="text-sm text-slate-500">{row.name}</p><p className="mt-1 text-2xl font-black">{row.count}</p></div>)}</div><p className="mt-4 text-sm font-semibold">Kredit arizalari: {analytics.creditApplications} · Faol pipeline: {analytics.pipelineCount}</p></div></section></>}
function AdminMetric({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 break-words text-2xl font-black">{value}</p></div>}
function AnalyticsTable({title,rows,amountLabel}:{title:string;rows:{name:string;count:number;amount:number}[];amountLabel:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black">{title}</h3>{rows.length?<div className="mt-3 overflow-x-auto"><table className="min-w-full text-sm"><thead><tr className="border-b text-left text-slate-500"><th className="py-2 pr-4">Nomi</th><th className="py-2 pr-4">Soni</th><th className="py-2 text-right">{amountLabel}</th></tr></thead><tbody>{rows.slice(0,10).map(row=><tr key={row.name} className="border-b last:border-0"><td className="py-2 pr-4 font-semibold">{row.name}</td><td className="py-2 pr-4">{row.count}</td><td className="py-2 text-right font-semibold">{money(row.amount)}</td></tr>)}</tbody></table></div>:<p className="mt-4 text-sm text-slate-500">Hozircha ma’lumot yo‘q.</p>}</div>}
