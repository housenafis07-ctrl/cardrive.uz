import { AdminCard } from "@/components/admin-ui";
import { AdminCatalogService } from "@/services/admin-catalog-service";
import { AdminSalesAnalyticsRepository } from "@/repositories/admin-sales-analytics-repository";
export const metadata={title:"Admin | Cardrive.uz"};
const money=(n:number)=>new Intl.NumberFormat("uz-UZ").format(Math.round(n))+" UZS";
type P={dateFrom?:string;dateTo?:string;bank?:string;dealer?:string;program?:string};
export default async function AdminPage({searchParams}:{searchParams:Promise<P>}){const p=await searchParams;const [counts,a]=await Promise.all([new AdminCatalogService().counts(),new AdminSalesAnalyticsRepository().get(p)]);return <><p className="text-sm font-bold text-amber-600">DASHBOARD</p><h1 className="mt-1 text-3xl font-black">Marketplace holati</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><AdminCard label="Brendlar" value={counts.brands}/><AdminCard label="Modellar" value={counts.models}/><AdminCard label="Avtomobillar" value={counts.cars}/><AdminCard label="Tavsiya etilganlar" value={counts.featured}/><AdminCard label="Mavjud" value={counts.available}/><AdminCard label="Tez orada" value={counts.comingSoon}/></div><section className="mt-10"><p className="text-sm font-bold text-amber-600">SOTUV ANALITIKASI</p><h2 className="mt-1 text-2xl font-black">Savdo va moliyalashtirish</h2><form method="get" className="mt-5 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:grid-cols-2 lg:grid-cols-5"><input name="dateFrom" type="date" defaultValue={p.dateFrom} className="rounded-lg border p-2"/><input name="dateTo" type="date" defaultValue={p.dateTo} className="rounded-lg border p-2"/><Select name="bank" value={p.bank} options={a.filterOptions.banks}/><Select name="dealer" value={p.dealer} options={a.filterOptions.dealers}/><Select name="program" value={p.program} options={a.filterOptions.programs}/><div className="flex gap-2 lg:col-span-5"><button className="rounded-lg bg-slate-950 px-4 py-2 text-sm font-bold text-white">Filtrlash</button><a href="/admin" className="rounded-lg border px-4 py-2 text-sm font-semibold">Tozalash</a></div></form><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Sotilgan avtomobillar" value={String(a.salesCount)}/><Metric label="Jami sotuv" value={money(a.salesAmount)}/><Metric label="Moliyalashtirish" value={money(a.financingAmount)}/><Metric label="Boshlang‘ich to‘lov" value={money(a.downPaymentAmount)}/><Metric label="O‘rtacha sotuv" value={money(a.averageSale)}/><Metric label="Kredit savdolari" value={String(a.creditSales)}/><Metric label="Rassrochka savdolari" value={String(a.installmentSales)}/><Metric label="Naqd savdolar" value={String(a.cashSales)}/></div><div className="mt-6 grid gap-5 xl:grid-cols-2"><Table title="Banklar bo‘yicha" rows={a.byBank} amount="Moliyalashtirish"/><Table title="Dilerlar bo‘yicha" rows={a.byDealer} amount="Sotuv summasi"/><Table title="Kredit dasturlari bo‘yicha" rows={a.byProgram} amount="Moliyalashtirish"/><Table title="Savdo turi bo‘yicha" rows={a.byPurchaseType} amount="Sotuv summasi"/></div><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black">Sotuv voronkasi</h3><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{a.byStatus.map(x=><div key={x.name} className="rounded-xl bg-slate-50 p-4"><p className="text-sm text-slate-500">{x.name}</p><p className="mt-1 text-2xl font-black">{x.count}</p></div>)}</div><p className="mt-4 text-sm font-semibold">Kredit arizalari: {a.creditApplications} · Faol pipeline: {a.pipelineCount}</p></div></section></>}
function Select({name,value,options}:{name:string;value?:string;options:string[]}){return <select name={name} defaultValue={value??""} className="rounded-lg border p-2"><option value="">Barchasi</option>{options.map(x=><option key={x}>{x}</option>)}</select>}
function Metric({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 break-words text-2xl font-black">{value}</p></div>}
function Table({title,rows,amount}:{title:string;rows:{name:string;count:number;amount:number}[];amount:string}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black">{title}</h3>{rows.length?<table className="mt-3 min-w-full text-sm"><thead><tr className="border-b text-left text-slate-500"><th className="py-2 pr-4">Nomi</th><th className="py-2 pr-4">Soni</th><th className="py-2 text-right">{amount}</th></tr></thead><tbody>{rows.slice(0,10).map(x=><tr key={x.name} className="border-b last:border-0"><td className="py-2 pr-4 font-semibold">{x.name}</td><td className="py-2 pr-4">{x.count}</td><td className="py-2 text-right font-semibold">{money(x.amount)}</td></tr>)}</tbody></table>:<p className="mt-4 text-sm text-slate-500">Hozircha ma’lumot yo‘q.</p>}</div>}
