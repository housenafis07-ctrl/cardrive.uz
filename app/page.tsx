import Link from "next/link";
import { Header, Footer } from "@/components/site-chrome";
import { HomeBannerCarousel } from "@/components/home-banner-carousel";
import { CarGrid, EmptyState } from "@/components/catalog-ui";
import { CatalogService } from "@/services/catalog-service";

export const dynamic = "force-dynamic";
export const revalidate = 0;
export const metadata = { title: "Cardrive.uz | Yangi avtomobillar", description: "O‘zbekistondagi yangi avtomobillar katalogi." };

export default async function HomePage() {
  const service = new CatalogService();
  const [featured, available, brands, banners, categories] = await Promise.all([service.getFeaturedCars(), service.getAvailableCars(), service.getActiveBrands(), service.getHomeBanners(), service.getHomeCategories()]);
  return <><Header/><main><section className="bg-slate-950 text-white"><div className="mx-auto max-w-7xl px-5 py-16"><p className="text-sm font-semibold text-amber-400">Yangi avtomobillar marketplace’i</p><h1 className="mt-4 text-4xl font-black tracking-tight sm:text-6xl">Yangi avtomobilingizni ishonch bilan tanlang.</h1><p className="mt-5 max-w-2xl text-lg text-slate-300">Brend, model, narx va texnik xususiyatlar bo‘yicha qidiring.</p><form action="/cars" className="mt-8 grid gap-3 rounded-2xl bg-white p-3 text-slate-950 shadow-xl sm:grid-cols-[1fr_auto]"><input name="q" placeholder="Brend yoki modelni qidiring..." className="rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-amber-500"/><button className="rounded-xl bg-amber-500 px-6 py-3 font-bold">Qidirish</button></form></div></section><HomeBannerCarousel banners={banners.data ?? []}/><section className="mx-auto max-w-7xl px-5 py-10"><div className="flex items-center justify-between"><h2 className="text-2xl font-black">Kategoriyalar</h2><Link href="/cars" className="text-sm font-bold underline">Barchasi</Link></div><div className="mt-5 flex gap-3 overflow-x-auto pb-2">{(categories.data ?? []).map((category)=><Link key={category.id} href={`/cars?category=${category.key}`} className="min-w-[190px] rounded-2xl border border-slate-200 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><span className="text-2xl">{category.icon}</span><p className="mt-3 font-bold">{category.name_uz}</p><p className="mt-1 text-xs text-slate-500">{category.name_ru}</p></Link>)}</div></section><section className="mx-auto max-w-7xl px-5 py-10"><div className="flex items-end justify-between"><div><p className="text-sm font-bold text-amber-600">TANLANGANLAR</p><h2 className="mt-2 text-3xl font-black">Tavsiya etilgan avtomobillar</h2></div><Link href="/cars?sort=featured" className="text-sm font-bold underline">Barchasi</Link></div><div className="mt-7">{featured.data?.length ? <CarGrid cars={featured.data}/> : <EmptyState title="Hozircha tavsiya etilgan avtomobillar yo‘q" action="Katalogga o‘tish"/>}</div></section><section className="bg-white"><div className="mx-auto max-w-7xl px-5 py-10"><h2 className="text-3xl font-black">Mavjud avtomobillar</h2><div className="mt-7">{available.data?.length ? <CarGrid cars={available.data}/> : <EmptyState title="Hozircha mavjud avtomobillar yo‘q"/>}</div></div></section><section className="mx-auto max-w-7xl px-5 py-16"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold text-amber-600">KATALOG</p><h2 className="mt-2 text-3xl font-black">Brendlar</h2><p className="mt-2 text-slate-500">Brendni tanlang va uning modellari bilan tanishing.</p></div></div>{brands.data?.length ? <div className="mt-7 grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6">{brands.data.map((brand)=><Link key={brand.id} href={`/cars?brand=${brand.slug}`} className="group flex min-h-36 flex-col items-center justify-center rounded-2xl border border-slate-200 bg-white p-5 text-center shadow-sm transition hover:-translate-y-0.5 hover:border-slate-300 hover:shadow-md"><div className="flex h-20 w-full items-center justify-center">{brand.logo_url?<img src={brand.logo_url} alt={brand.name} className="max-h-16 max-w-[110px] object-contain transition group-hover:scale-105" loading="lazy" decoding="async" referrerPolicy="no-referrer"/>:<span className="text-lg font-black uppercase text-slate-600">{brand.name}</span>}</div><p className="mt-3 text-sm font-bold text-slate-900">{brand.name}</p><p className="mt-1 text-xs font-medium text-slate-500">{brand.model_count} model</p></Link>)}</div>:<EmptyState title="Hozircha faol brendlar yo‘q"/>}</section></main><Footer/></>;
}
