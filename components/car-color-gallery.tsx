"use client";
import { useMemo, useState } from "react";
type Locale="uz"|"ru"; type Color={id:string;name_uz:string;name_ru:string;hex_code:string}; type Image={public_url:string|null;alt_text:string|null;is_primary:boolean;sort_order:number;color_id?:string|null;car_colors?:Color|Color[]|null};
function getColor(image:Image){if(!image.car_colors)return null;return Array.isArray(image.car_colors)?image.car_colors[0]??null:image.car_colors}
export function CarColorGallery({images,carName,locale="uz"}:{images:Image[];carName:string;locale?:Locale}){const[selectedColorId,setSelectedColorId]=useState<string|null>(null);const[selectedImageUrl,setSelectedImageUrl]=useState<string|null>(null);const colors=useMemo(()=>{const map=new Map<string,Color>();for(const image of images){const color=getColor(image);if(color&&!map.has(color.id))map.set(color.id,color)}return[...map.values()]},[images]);const sortedImages=useMemo(()=>[...images].sort((a,b)=>Number(b.is_primary)-Number(a.is_primary)||a.sort_order-b.sort_order),[images]);const selectedColorImage=selectedColorId?sortedImages.find(image=>image.color_id===selectedColorId&&image.public_url):null;const mainImage=selectedImageUrl?sortedImages.find(image=>image.public_url===selectedImageUrl):selectedColorImage??sortedImages[0];const visibleImages=selectedColorId?sortedImages.filter(image=>image.color_id===selectedColorId&&image.public_url):sortedImages.filter(image=>image.public_url);const text=locale==="ru"?{soon:"Фото автомобиля скоро появится",missing:"Для этого цвета фото не загружено",color:"Цвет",noImage:"фото ещё не привязано"}:{soon:"Avtomobil rasmi tez orada",missing:"Bu rang uchun rasm yuklanmagan",color:"Rang",noImage:"rasm hali biriktirilmagan"};if(!images.length)return <div className="flex aspect-[16/10] items-center justify-center rounded-3xl bg-slate-100 text-slate-500">{text.soon}</div>;return <div><div className="aspect-[16/10] overflow-hidden rounded-3xl bg-white">{mainImage?.public_url?<img src={mainImage.public_url} alt={mainImage.alt_text??carName} className="h-full w-full object-contain"/>:<div className="flex h-full items-center justify-center text-slate-500">{text.missing}</div>}</div>{colors.length>0&&<div className="mt-5"><p className="text-base font-black">{text.color}</p><div className="mt-3 flex flex-wrap gap-3">{colors.map(color=>{const hasImage=sortedImages.some(image=>image.color_id===color.id&&image.public_url);const active=selectedColorId===color.id;const name=locale==="ru"?color.name_ru:color.name_uz;return <button key={color.id} type="button" title={`${name}${hasImage?"":` — ${text.noImage}`}`} onClick={()=>{setSelectedColorId(color.id);setSelectedImageUrl(null)}} className={`flex items-center gap-2 rounded-full border px-3 py-2 text-sm font-semibold transition ${active?"border-slate-950 ring-2 ring-slate-300":"border-slate-200"} ${!hasImage?"opacity-50":""}`}><span className="h-5 w-5 rounded-full border border-slate-300" style={{backgroundColor:color.hex_code}}/><span>{name}</span></button>})}</div></div>}{visibleImages.length>1&&<div className="mt-4 grid grid-cols-4 gap-3">{visibleImages.slice(0,8).map(image=><button type="button" key={`${image.public_url}-${image.sort_order}`} onClick={()=>setSelectedImageUrl(image.public_url)} className="aspect-square overflow-hidden rounded-xl border bg-slate-100">{image.public_url&&<img src={image.public_url} alt={image.alt_text??carName} className="h-full w-full object-cover"/>}</button>)}</div>}</div>}
