"use client";
import { FormEvent,useState } from "react";
import { requestCustomerOtpAction,verifyCustomerOtpAction,createCustomerProfileAction } from "@/app/account/actions";
import type { OtpPurpose } from "@/types/domain";
import type { Locale } from "@/lib/i18n";
import { translations } from "@/lib/i18n";
type Step="phone"|"otp"|"profile";
export function PhoneOtpAuth({purpose,onAuthenticated,title,locale="uz"}:{purpose:OtpPurpose;onAuthenticated:()=>void;title?:string;locale?:Locale}){const t=translations[locale];const[step,setStep]=useState<Step>("phone");const[phone,setPhone]=useState("+998");const[code,setCode]=useState("");const[firstName,setFirstName]=useState("");const[lastName,setLastName]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function onPhone(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);const r=await requestCustomerOtpAction(phone,purpose);setLoading(false);if(r.status==="error"){setError(r.message);return;}setStep("otp");}async function onCode(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);const r=await verifyCustomerOtpAction(phone,code,purpose);setLoading(false);if(r.status==="error"){setError(r.message);return;}if(r.status==="new_customer"){setStep("profile");return;}onAuthenticated();}async function onProfile(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const fullName=`${firstName.trim()} ${lastName.trim()}`.trim();if(!firstName.trim()||!lastName.trim()){setError(t.authInvalidName);return;}setLoading(true);const r=await createCustomerProfileAction(phone,fullName);setLoading(false);if(r.status==="error"){setError(r.message);return;}onAuthenticated();}if(step==="phone")return <form onSubmit={onPhone} className="space-y-4"><div><p className="text-lg font-bold">{title??t.authTitle}</p><p className="mt-1 text-sm text-slate-500">{t.authSms}</p></div><input required type="tel" inputMode="tel" autoComplete="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="+998901234567" className="w-full rounded-lg border px-3 py-2"/>{error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-slate-950 py-3 font-bold text-white disabled:opacity-50">{loading?t.authSending:t.authSend}</button></form>;if(step==="otp")return <form onSubmit={onCode} className="space-y-4"><p className="text-lg font-bold">{t.authEnter}</p><p className="text-sm text-slate-500">{phone} {t.authSent}</p><input required type="text" inputMode="numeric" autoComplete="one-time-code" value={code} onChange={e=>setCode(e.target.value)} placeholder="123456" className="w-full rounded-lg border px-3 py-2"/>{error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-slate-950 py-3 font-bold text-white disabled:opacity-50">{loading?t.authChecking:t.authVerify}</button><button type="button" onClick={()=>{setStep("phone");setCode("");setError("")}} className="w-full text-sm font-semibold text-slate-500 underline">{t.authChange}</button></form>;return <form onSubmit={onProfile} className="space-y-4"><p className="text-lg font-bold">{t.authProfile}</p><input required value={firstName} onChange={e=>setFirstName(e.target.value)} placeholder={t.authFirstName} className="w-full rounded-lg border px-3 py-2"/><input required value={lastName} onChange={e=>setLastName(e.target.value)} placeholder={t.authLastName} className="w-full rounded-lg border px-3 py-2"/>{error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-slate-950 py-3 font-bold text-white disabled:opacity-50">{loading?t.authSaving:t.authContinue}</button></form>}
