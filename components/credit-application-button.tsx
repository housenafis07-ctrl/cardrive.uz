"use client";
import {useState} from "react";
import {submitCreditApplicationAction} from "@/app/account/credit-actions";
export function CreditApplicationButton({orderId,financingProgramId}:{orderId:string;financingProgramId:string}){const[loading,setLoading]=useState(false);const[message,setMessage]=useState("");async function submit(){setLoading(true);setMessage("");const fd=new FormData();fd.set("orderId",orderId);fd.set("financingProgramId",financingProgramId);const result=await submitCreditApplicationAction(fd);setLoading(false);if(result.status==="error"){setMessage(result.message);return}setMessage(result.status==="already_exists"?"Ariza allaqachon mavjud.":"Kredit arizasi qabul qilindi. Menejer bankka yuborish uchun tekshiradi.");}return <div className="mt-5 space-y-3"><button type="button" onClick={submit} disabled={loading} className="w-full rounded-full bg-slate-950 py-3 font-bold text-white disabled:opacity-50">{loading?"Yuborilmoqda...":"Kreditga ariza berish"}</button>{message&&<p className="rounded-lg bg-slate-50 p-3 text-sm font-semibold text-slate-700">{message}</p>}</div>}
