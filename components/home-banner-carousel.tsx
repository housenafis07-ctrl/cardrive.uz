"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import type {Locale} from "@/lib/i18n";
type Banner={id:string;title_uz:string;title_ru:string;description_uz?:string|null;description_ru?:string|null;image_url:string;cta_uz?:string|null;cta_ru?:string|null;href?:string|null};
export function HomeBannerCarousel({banners,locale="uz"}:{banners:Banner[];locale?:Locale}){const[index,setIndex]=useState(0);useEffect(()=>{if(banners.length<2)return;const timer=window.setInterval(()=>setIndex(v=>(v+1)%banners.length),5000);return()=>window.clearInterval(timer)},[banners.length]);if(!banners.length)return null;const b=banners[index];const title=locale==="ru"?b.title_ru:b.title_uz,description=locale==="ru"?b.description_ru:b.description_uz,cta=locale==="ru"?b.cta_ru:b.cta_uz;return <section className="mx-auto max-w-7xl px-5 pt-6"><div className="relative min-h-[260px] overflow-hidden rounded-3xl bg-slate-900"><img src={b.image_url} alt={title} className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-r from-slate-950/85 via-slate-950/45 to-transparent"/><div className="relative z-10 flex min-h-[260px] max-w-xl flex-col justify-center p-8 text-white sm:p-12"><h2 className="text-3xl font-black sm:text-5xl">{title}</h2>{description&&<p className="mt-4 max-w-lg text-base text-slate-100 sm:text-lg">{description}</p>}{b.href&&cta&&<Link href={b.href} className="mt-6 inline-flex w-fit rounded-full bg-amber-500 px-5 py-3 font-bold text-slate-950">{cta}</Link>}</div>{banners.length>1&&<><button type="button" aria-label={locale==="ru"?"Предыдущий баннер":"Oldingi banner"} onClick={()=>setIndex((index-1+banners.length)%banners.length)} className="absolute left-4 top-1/2 z-20 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-xl text-slate-950">‹</button><button type="button" aria-label={locale==="ru"?"Следующий баннер":"Keyingi banner"} onClick={()=>setIndex((index+1)%banners.length)} className="absolute right-4 top-1/2 z-20 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-xl text-slate-950">›</button></>}</div></section>}
