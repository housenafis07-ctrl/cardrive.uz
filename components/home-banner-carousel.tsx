"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
type Banner={id:string;title_uz:string;title_ru:string;description_uz?:string|null;description_ru?:string|null;image_url:string;cta_uz?:string|null;cta_ru?:string|null;href?:string|null};
export function HomeBannerCarousel({banners}:{banners:Banner[]}){const[index,setIndex]=useState(0);useEffect(()=>{if(banners.length<2)return;const timer=window.setInterval(()=>setIndex((value)=>(value+1)%banners.length),5000);return()=>window.clearInterval(timer)},[banners.length]);if(!banners.length)return null;const banner=banners[index];return <section className="mx-auto max-w-7xl px-5 pt-6"><div className="relative min-h-[260px] overflow-hidden rounded-3xl bg-slate-900"><img src={banner.image_url} alt={banner.title_uz} className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-r from-slate-950/85 via-slate-950/45 to-transparent"/><div className="relative z-10 flex min-h-[260px] max-w-xl flex-col justify-center p-8 text-white sm:p-12"><h2 className="text-3xl font-black sm:text-5xl">{banner.title_uz}</h2>{banner.description_uz&&<p className="mt-4 max-w-lg text-base text-slate-100 sm:text-lg">{banner.description_uz}</p>}{banner.href&&banner.cta_uz&&<Link href={banner.href} className="mt-6 inline-flex w-fit rounded-full bg-amber-500 px-5 py-3 font-bold text-slate-950">{banner.cta_uz}</Link>}</div>{banners.length>1&&<><button type="button" aria-label="Oldingi banner" onClick={()=>setIndex((index-1+banners.length)%banners.length)} className="absolute left-4 top-1/2 z-20 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-xl text-slate-950">‹</button><button type="button" aria-label="Keyingi banner" onClick={()=>setIndex((index+1)%banners.length)} className="absolute right-4 top-1/2 z-20 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-xl text-slate-950">›</button><div className="absolute bottom-4 left-1/2 z-20 flex -translate-x-1/2 gap-1.5">{banners.map((item,dotIndex)=><button key={item.id} type="button" aria-label={`Banner ${dotIndex+1}`} onClick={()=>setIndex(dotIndex)} className={`h-2 rounded-full transition-all ${dotIndex===index?"w-7 bg-white":"w-2 bg-white/60"}`}/>)}</div></>}</div></section>}
