import Link from "next/link";
import type { CatalogQuery } from "@/features/catalog/catalog-query";
export function FilterPanel({ query, brands, bodyTypes }: { query: CatalogQuery; brands: Array<{name:string;slug:string}>; bodyTypes: string[] }) { return <form action="/cars" className="space-y-4 rounded-2xl border border-slate-200 bg-white p-4"><label className="block text-sm font-semibold">Qidirish<input name="q" defaultValue={query.q} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" placeholder="Avtomobil nomi"/></label><label className="block text-sm font-semibold">Brend<select name="brand" defaultValue={query.brand} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="">Barchasi</option>{brands.map((brand)=><option key={brand.slug} value={brand.slug}>{brand.name}</option>)}</select></label><label className="block text-sm font-semibold">Kuzov<select name="body" defaultValue={query.body} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="">Barchasi</option>{bodyTypes.map((body)=><option key={body} value={body}>{body}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><label className="text-sm font-semibold">Min. narx<input name="minPrice" type="number" defaultValue={query.minPrice} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"/></label><label className="text-sm font-semibold">Maks. narx<input name="maxPrice" type="number" defaultValue={query.maxPrice} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"/></label></div><label className="block text-sm font-semibold">Saralash<select name="sort" defaultValue={query.sort} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="recommended">Tavsiya etilgan</option><option value="price_asc">Narx: arzon</option><option value="price_desc">Narx: qimmat</option><option value="newest">Yangi yil</option><option value="featured">Tavsiya qilingan</option></select></label><button className="w-full rounded-full bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">Qo‘llash</button><Link href="/cars" className="block text-center text-sm font-semibold text-slate-600 underline">Tozalash</Link></form>; }
export function Pagination({ page, total }: { page: number; total: number }) { const pages=Math.ceil(total/12); if(pages<2)return null; const prev=Math.max(1,page-1), next=Math.min(pages,page+1); return <nav className="mt-8 flex items-center justify-center gap-3" aria-label="Sahifalash"><Link href={"/cars?page="+prev} aria-disabled={page===1} className="rounded-lg border px-3 py-2">Oldingi</Link><span className="text-sm text-slate-600">{page} / {pages}</span><Link href={"/cars?page="+next} aria-disabled={page===pages} className="rounded-lg border px-3 py-2">Keyingi</Link></nav>; }
