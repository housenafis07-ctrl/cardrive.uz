import Link from "next/link"; import type {Locale} from "@/lib/i18n"; import type {CatalogQuery} from "@/features/catalog/catalog-query";
export function FilterPanel({query,brands,bodyTypes,locale="uz"}:{query:CatalogQuery;brands:Array<{name:string;slug:string}>;bodyTypes:string[];locale?:Locale}){const ru=locale==="ru";return <form action="/cars" className="space-y-4 rounded-2xl border border-slate-200 bg-white p-4"><label className="block text-sm font-semibold">{ru?"Поиск":"Qidirish"}<input name="q" defaultValue={query.q} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" placeholder={ru?"Название автомобиля":"Avtomobil nomi"}/></label><label className="block text-sm font-semibold">{ru?"Бренд":"Brend"}<select name="brand" defaultValue={query.brand} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="">{ru?"Все":"Barchasi"}</option>{brands.map(brand=><option key={brand.slug} value={brand.slug}>{brand.name}</option>)}</select></label><label className="block text-sm font-semibold">{ru?"Кузов":"Kuzov"}<select name="body" defaultValue={query.body} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="">{ru?"Все":"Barchasi"}</option>{bodyTypes.map(body=><option key={body} value={body}>{body}</option>)}</select></label><div className="grid grid-cols-2 gap-2"><label className="text-sm font-semibold">{ru?"Мин. цена":"Min. narx"}<input name="minPrice" type="number" defaultValue={query.minPrice} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"/></label><label className="text-sm font-semibold">{ru?"Макс. цена":"Maks. narx"}<input name="maxPrice" type="number" defaultValue={query.maxPrice} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"/></label></div><label className="block text-sm font-semibold">{ru?"Сортировка":"Saralash"}<select name="sort" defaultValue={query.sort} className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2"><option value="recommended">{ru?"Рекомендуемые":"Tavsiya etilgan"}</option><option value="price_asc">{ru?"Цена: по возрастанию":"Narx: arzon"}</option><option value="price_desc">{ru?"Цена: по убыванию":"Narx: qimmat"}</option><option value="newest">{ru?"Новые":"Yangi yil"}</option><option value="featured">{ru?"Рекомендуемые":"Tavsiya qilingan"}</option></select></label><button className="w-full rounded-full bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white">{ru?"Применить":"Qo‘llash"}</button><Link href="/cars" className="block text-center text-sm font-semibold text-slate-600 underline">{ru?"Сбросить":"Tozalash"}</Link></form>}
export function Pagination({page,total,locale="uz"}:{page:number;total:number;locale?:Locale}){const pages=Math.ceil(total/12);if(pages<2)return null;const prev=Math.max(1,page-1),next=Math.min(pages,page+1),ru=locale==="ru";return <nav className="mt-8 flex items-center justify-center gap-3" aria-label={ru?"Пагинация":"Sahifalash"}><Link href={`/cars?page=${prev}`} className="rounded-lg border px-3 py-2">{ru?"Назад":"Oldingi"}</Link><span className="text-sm text-slate-600">{page} / {pages}</span><Link href={`/cars?page=${next}`} className="rounded-lg border px-3 py-2">{ru?"Далее":"Keyingi"}</Link></nav>}
