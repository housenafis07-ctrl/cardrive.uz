export type FormatterLocale = "uz" | "ru";
export function formatPrice(amount: number, currency: string, locale: FormatterLocale = "uz"): string { if (currency === "UZS") return new Intl.NumberFormat(locale === "ru" ? "ru-RU" : "uz-UZ", { maximumFractionDigits: 0 }).format(amount) + (locale === "ru" ? " сум" : " soʻm"); return new Intl.NumberFormat(locale === "ru" ? "ru-RU" : "uz-UZ", { style: "currency", currency, maximumFractionDigits: 0 }).format(amount); }
export function stockLabel(status: string, locale: FormatterLocale = "uz"): string { const labels = locale === "ru" ? { available:"В наличии", coming_soon:"Скоро в наличии", reserved:"Забронирован", sold:"Продан" } : { available:"Mavjud", coming_soon:"Tez orada", reserved:"Band qilingan", sold:"Sotilgan" }; return labels[status as keyof typeof labels] ?? (locale === "ru" ? "Продан" : "Sotilgan"); }
