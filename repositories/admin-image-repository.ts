import "server-only";
import { createServiceRoleClient } from "@/supabase/server";
export class AdminImageRepository { private client(){return createServiceRoleClient();} async list(carId:string){return this.client().from("car_images").select("*").eq("car_id",carId).order("is_primary",{ascending:false}).order("sort_order");} async upload(carId:string,file:File){const safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,"-");const path="cars/"+carId+"/"+crypto.randomUUID()+"-"+safeName;const client=this.client();const upload=await client.storage.from("car-images").upload(path,file,{contentType:file.type,upsert:false});if(upload.error)return {error:upload.error};const {data}=client.storage.from("car-images").getPublicUrl(path);return client.from("car_images").insert({car_id:carId,storage_path:path,public_url:data.publicUrl,alt_text:null,sort_order:0,is_primary:false}).select().single();} async setPrimary(carId:string,imageId:string){const client=this.client();await client.from("car_images").update({is_primary:false}).eq("car_id",carId);return client.from("car_images").update({is_primary:true}).eq("id",imageId).eq("car_id",carId);} async remove(carId:string,imageId:string){const client=this.client();const current=await client.from("car_images").select("storage_path").eq("id",imageId).eq("car_id",carId).single();if(current.data)await client.storage.from("car-images").remove([current.data.storage_path]);return client.from("car_images").delete().eq("id",imageId).eq("car_id",carId);} }
